package eapli.alsafe.antlr.semantic;

/**
 * Categories of names and semantic objects stored during analysis.
 */
public enum SymbolKind {
    FLIGHT,
    ROUTE,
    AIRCRAFT,
    AIRPORT,
    LEG,
    SEGMENT
}
